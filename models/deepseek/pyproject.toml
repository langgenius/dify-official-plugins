[project]
name = "deepseek"
version = "0.1.0"
description = "Dify model provider for the official DeepSeek API"
readme = "README.md"
requires-python = ">=3.12"

# Managed with uv; refresh the lockfile with `uv lock`.
dependencies = [
    "dify_plugin>=0.10.2",
]

# uv run black . -C -l 100 && uv run ruff check --fix
[dependency-groups]
dev = [
    "pyrefly>=1.2.0",
    "pytest>=9.1.1",
]
