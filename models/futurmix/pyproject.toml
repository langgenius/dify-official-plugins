[project]
name = "dify-plugin-futurmix"
version = "0.0.1"
description = "FuturMix AI Gateway plugin for Dify"
requires-python = ">=3.12"
dependencies = [
    "dify-plugin>=0.5.0",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
