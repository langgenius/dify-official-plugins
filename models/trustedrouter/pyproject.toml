[project]
name = "trustedrouter"
version = "0.0.1"
description = "TrustedRouter model provider plugin for Dify"
readme = "README.md"
requires-python = ">=3.12"

# Managed with uv; refresh the lockfile with `uv lock`.
dependencies = [
    "dify_plugin>=0.9.0",
]

[dependency-groups]
dev = []
