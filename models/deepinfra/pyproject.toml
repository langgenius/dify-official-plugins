[project]
name = "deepinfra"
version = "0.1.0"
description = "DeepInfra model provider for Dify"
requires-python = ">=3.12"

dependencies = [
    "dify_plugin>=0.9.0",
    "openai>=1.61.0",
    "numpy>=1.26.0",
    "tiktoken>=0.8.0",
]

[dependency-groups]
dev = []
