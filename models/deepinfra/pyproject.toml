[project]
name = "deepinfra"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["dify_plugin>=0.9.0","httpx>=0.28.1","openai>=2.38.0"]
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"