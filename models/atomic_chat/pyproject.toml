[project]
name = "atomic-chat"
version = "0.1.0"
description = "Atomic Chat model provider for Dify"
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
    "dify_plugin>=0.9.0",
    "httpx>=0.28.1",
    "openai>=2.38.0",
    "requests>=2.34.2",
]

[dependency-groups]
dev = []
