[project]
name = "llmman"
version = "0.0.1"
description = "Dify model provider for llmman (https://github.com/llmmanorg/llmman)"
readme = "README.md"
requires-python = ">=3.12"

# Managed with uv; refresh the lockfile with `uv lock`.
dependencies = [
    "dify_plugin>=0.10.0",
]

[dependency-groups]
dev = []
