[project]
name = "mimo"
version = "0.1.0"
description = "Xiaomi MiMo model provider for Dify"
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
    "dify_plugin>=0.10.0",
    "multidict<6.9.1",
]

[dependency-groups]
dev = []
