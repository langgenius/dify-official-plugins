[project]
name = "dify-gandr"
version = "0.1.0"
description = "Gandr TTS models for Dify: an OpenAI-compatible speech API with 23 languages and six stock voices."
readme = "README.md"
requires-python = ">=3.12"

# Managed with uv; refresh the lockfile with `uv lock`.
dependencies = [
    "dify_plugin>=0.9.0",
    "requests>=2.32.0",
]

# uv run black . -C -l 100 && uv run ruff check --fix
[dependency-groups]
dev = []