[project]
name = "openai-api-compatible-GnRzR6cEy"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"

# Refresh the lockfile with: uv lock
dependencies = [
    # 0.10.0 introduces get_current_session(), required to resolve the
    # current Dify app_id for OpenAI-compatible request headers.
    "dify_plugin>=0.10.0",
    "httpx>=0.28.1",
    "openai>=2.38.0",
    "requests>=2.34.2",
    "setuptools>=83.0.0",
    "tiktoken>=0.13.0",
]

# uv run black . -C -l 100
# uv run ruff check --fix
[dependency-groups]
dev = [
    "black>=26.5.1",
    "pytest>=9.0.3",
    "ruff>=0.15.14",
]
