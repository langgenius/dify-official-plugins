[project]
name = "liteparse"
version = "0.1.0"
description = "High-performance document parsing powered by Docling."
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
    "dify_plugin>=0.4.0",
    "docling",
]

[dependency-groups]
dev = []
