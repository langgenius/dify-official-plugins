[project]
name = "zoom-tool"
version = "0.1.0"
description = "Zoom meeting management plugin for Dify"
readme = "README.md"
requires-python = ">=3.12"

# Managed with uv; refresh the lockfile with `uv lock`.
dependencies = [
    "dify-plugin~=0.7.1",
    "requests>=2.32.5,<2.33.0",
    "werkzeug>=3.1.0",
]

[dependency-groups]
dev = []
