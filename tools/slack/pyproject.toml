[project]
name = "dify-slack"
version = "0.0.1"
description = "Dify plugin for Slack — feature parity with the n8n Slack node"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dify_plugin>=0.5.0,<0.8.0",
    "requests>=2.20.0",
]

[dependency-groups]
dev = []
