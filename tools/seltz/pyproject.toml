[project]
name = "dify-seltz"
version = "0.0.1"
description = "Seltz Web Knowledge API plugin for Dify"
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
    "dify_plugin>=0.3.0,<0.6.0",
    "seltz",
]

[dependency-groups]
dev = []
