# Copy to .env for local plugin debugging (dify plugin daemon remote install).
# These are NOT the Resemble API key — that is entered in the Dify UI as a credential.
INSTALL_METHOD=remote
REMOTE_INSTALL_HOST=debug.dify.ai
REMOTE_INSTALL_PORT=5003
REMOTE_INSTALL_KEY=__your_dify_debug_key__
