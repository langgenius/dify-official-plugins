[project]
name = "resemble"
version = "0.1.0"
description = "Resemble Detect + Intelligence tools for Dify (deepfake detection, intelligence, source tracing, watermarking)."
readme = "README.md"
requires-python = ">=3.12"

# Refresh the lockfile with: uv lock
dependencies = [
    "dify_plugin>=0.9.0",
    "requests>=2.32.0",
]

[dependency-groups]
dev = []
