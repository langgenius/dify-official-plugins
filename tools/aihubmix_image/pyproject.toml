[project]
name = "aihubmix_image"
version = "0.2.0"
description = "AIHubMix image generation and editing for Dify, driven by the gateway's live model schemas"
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
    "dify_plugin>=0.10.2",
    "requests>=2.34.2",
]

# uv run black . -C -l 100 && uv run ruff check --fix
[dependency-groups]
dev = []
